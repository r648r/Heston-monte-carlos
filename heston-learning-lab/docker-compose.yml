version: '3.8'

services:
  jupyter:
    build: .
    container_name: heston-learning-lab
    ports:
      - "8888:8888"
    volumes:
      - ./notebooks:/workspace/notebooks
    environment:
      - JUPYTER_ENABLE_LAB=yes
    stdin_open: true
    tty: true
